{% comment %}
  This section renders standard store pages (About Us, Privacy Policy, Refund Policy,
  Shipping Policy, Terms of Service, Contact Us, FAQ) inside a centered 1200px container
  with light #F5F6F8 background and zero black side margins.
{% endcomment %}

<div class="page__wrapper full-width">
  <div class="page__container">
    <h1 class="page__title">{{ page.title }}</h1>
    <div class="page__content">
      {% case page.handle %}
        {% when 'about-us' %}
          {% render 'page-about-us' %}
        {% when 'privacy-policy' %}
          {% render 'page-privacy-policy' %}
        {% when 'refund-policy', 'refund-and-return-policy' %}
          {% render 'page-refund-policy' %}
        {% when 'shipping-policy' %}
          {% render 'page-shipping-policy' %}
        {% when 'terms-of-service', 'terms-and-conditions' %}
          {% render 'page-terms-of-service' %}
        {% when 'contact-us', 'contact' %}
          {% render 'page-contact-us' %}
        {% when 'faq', 'faqs' %}
          {% render 'page-faq' %}
        {% else %}
          {{ page.content }}
      {% endcase %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "t:general.page",
  "settings": []
}
{% endschema %}
